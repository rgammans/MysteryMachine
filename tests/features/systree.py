# -*- coding: utf-8 -*-
from lettuce import step

@step(u'and the "" packfile is loaded')
def and_the_packfile_is_loaded(step):
    assert False, 'This step must be implemented'


@step(u'then the root node is named ""')
def then_the_root_node_is_named(step):
    assert False, 'This step must be implemented'

@step(u'when i right-click on the root node')
def when_i_right_click_on_the_root_node(step):
    assert False, 'This step must be implemented'


@step(u'I should see the follow menu entries:-')
def i_should_see_the_follow_menu_entries(step):
    assert False, 'This step must be implemented'

@step(u'when i exapnd the root-node')
def when_i_exapnd_the_root_node(step):
    assert False, 'This step must be implemented'

@step(u'and click on category nodel')
def and_click_on_category_nodel(step):
    assert False, 'This step must be implemented'
